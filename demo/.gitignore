# 실행 중 생기는 것들 — 템플릿만 커밋한다
*/.mm/steps/
*/.mm/state.json
*/.mm/pending.json
*/.mm/capture.log
*/.mm-cache/
*/check-result.json

# demo/state/ 는 전부 실행 산출물 (mm state / mm baseline / market_receipt 가 쓴다).
# 폴더는 scripts/demo-state.ts 가 필요할 때 만든다. 형식은 같은 파일의 CompareState 인터페이스 참고.
state/
